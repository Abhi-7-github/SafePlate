# Server
# Local port for development.
# On Render (and many other hosts), the platform injects PORT automatically.
PORT=5050

# Deployed URLs (optional, for documentation/CORS)
# CLIENT_ORIGIN=https://safeplate7.netlify.app
# PUBLIC_BASE_URL=https://safeplate-h5oz.onrender.com

# CORS (optional)
# If set, only these origins are allowed. Comma-separated.
# CORS_ORIGIN=https://safeplate7.netlify.app,http://localhost:5173

# Optional AI (OpenAI-compatible)
# If enabled, the server will call the model and validate that it returns the exact
# Decision Card format (and will fall back to heuristics if not).
# Note: Translating the Decision Card into Indian languages uses the same AI provider.
# So if you want non-English Decision Cards, you must configure an AI key.
#
# USE_AI=true
# AI_ONLY=true
# AI_PROVIDER=gemini
# OPENAI_API_KEY=your_key_here
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com

# Gemini (recommended if you have a Gemini API key)
# GEMINI_API_KEY=your_key_here
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com

# Optional MongoDB (only if you need persistence later)
# MONGO_URI=mongodb://127.0.0.1:27017/safeplate
